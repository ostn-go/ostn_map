name: custom_zoomable_floorplan
description: A new Flutter application.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  cached_network_image: ^3.0.0

  cupertino_icons: ^1.0.3
  provider: ^6.0.5
  flutter_timer_countdown: ^1.0.5
  amazon_cognito_identity_dart_2: ^1.0.5
  flutter_aws_s3_client: ^0.5.3
  panorama: ^0.4.1
  logger: ^1.0.0
  amplify_flutter: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - images/

